# CerebroCalm Environment Configuration
# Default application operates 100% locally with zero external API keys required!

# Optional: External LLM provider keys for guarded educational assistant (Leave blank for default local heuristic coach)
# OPENAI_API_KEY=
# GEMINI_API_KEY=

# Node Environment
NODE_ENV=development
